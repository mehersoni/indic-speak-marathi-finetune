# Core dependencies for indic-speak-marathi verified for Python 3.10 and CUDA 12.x.
# Provides libraries for audio processing, datasets, and LoRA training.
# Note: torch is omitted because Kaggle environments ship a GPU-matched torch
# preinstalled; reinstalling it risks breaking the CUDA match.

transformers>=5.0.0
peft>=0.12.0
accelerate>=0.33.0
snac>=1.2.1
soundfile>=0.12.1
datasets>=2.20.0
pandas>=2.2.0,<3.0.0
pyarrow>=15.0.0
huggingface_hub>=0.24.0
pyyaml>=6.0
